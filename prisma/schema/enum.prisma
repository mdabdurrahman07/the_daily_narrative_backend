enum activeStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  AUTHOR
  USER
  ADMIN
}

enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVE
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}
